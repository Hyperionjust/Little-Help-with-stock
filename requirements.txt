# ── 核心（引擎必需）────────────────────────────────────────────
requests>=2.31
jsonschema>=4.0          # 契约校验（闸1/闸2）——缺它回归会报 AMBER，绝不静默通过
openpyxl>=3.1            # Excel 底稿（活公式）

# ── 数据源（按需，缺失时降级链自动跳过）──────────────────────
akshare>=1.12            # A股财报主力
efinance>=0.5
baostock>=0.8
yfinance>=0.2            # 美股/港股

# ── 分析与出版（阶段 3-5 启用）──────────────────────────────
pandas>=2.0
numpy>=1.24
matplotlib>=3.7          # 图表生成
weasyprint>=60           # HTML→PDF 唯一引擎（不用 Playwright：CSS 依赖 paged-media 特性）
pypdf>=4.0               # PDF 真页数校验
Jinja2>=3.1              # 版式模板

# ── 开发/自测 ────────────────────────────────────────────────
pytest>=7.0
